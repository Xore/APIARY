# Elasticsearch client. 8.19.3 -- re-verified live (2026-08-26, #2090)
# against the server the fleet actually runs since #1408/#1410: 9.5.1,
# digest-pinned in honeypot-init/honeypot-elk's compose.yml. A server
# accepts clients one major back but none forward, so this client's
# compatible-with=8 header is accepted by the 9.x server where a 9.x
# client's compatible-with=9 was rejected by the retired 8.13.4 one --
# full info/ping/index/search/delete roundtrip PASS, recorded in
# docs/runtime-compatibility-record.md. Held rather than bumped: see
# ml-worker/requirements.txt's comment for the incident history and the
# ignore-rule doctrine behind that.
elasticsearch==8.19.3

# Strict output contracts and JSON Schema sent to Ollama structured output.
pydantic==2.13.5

# Local-only Ollama client. The worker disables environment proxy use and
# redirects before sending attacker-derived text.
requests==2.34.2
