# Minimal Redis config for tanner_redis honeypot service.
# Bind only to the compose network interface (not 0.0.0.0).
bind 0.0.0.0
protected-mode no
port 6379
timeout 0
tcp-keepalive 300
loglevel notice
databases 16
save ""
appendonly no
maxmemory 64mb
maxmemory-policy allkeys-lru
