# Elasticsearch client. 8.19.3, not the newer 9.4.1 -- same pin, same
# reasoning, as ml-worker/requirements.txt: the 9.x client enforces
# Elastic's N/N-1 major-version compatibility policy at the protocol level
# (a `compatible-with=9` Accept header), and the server this talks to is
# pinned to 8.13.4 (docker-compose.yml's elasticsearch service), which
# rejects that header outright. See ml-worker/requirements.txt's own
# comment for the two live Dependabot regressions this exact pin already
# caught -- .github/dependabot.yml's ignore rule covers this directory too.
elasticsearch==8.19.3
# 2.34.2, not 2.32.3: dependency-review flagged two moderate CVEs in 2.32.3
# (GHSA-9hjg-9r4m-mvj7 .netrc credentials leak, GHSA-gc5v-m9x4-r6x2 insecure
# temp file reuse in extract_zipped_paths()) on this PR's first CI run.
requests==2.34.2
loguru==0.7.3
