# Elasticsearch client. 8.19.3, held through the fleet's server migration
# 8.13.4 -> 8.19.20 -> 9.5.1 (#1408/#1410) and re-verified against the
# server actually deployed today: 9.5.1, digest-pinned in
# arcane/home/honeypot-init/compose.yml and honeypot-elk/compose.yml.
# Re-verified live 2026-08-26 (#2090): full info/ping/index/search/delete
# roundtrip from this exact client version against that server -- PASS,
# recorded in docs/runtime-compatibility-record.md. Elastic's N/N-1
# compatibility policy is symmetric: a server accepts clients one major
# back but none forward, so the 9.x server accepts this client's
# compatible-with=8 header, while the retired 8.13.4 server rejected a
# 9.x client's compatible-with=9 outright
# (media_type_header_exception) -- confirmed live at the time against a
# disposable 8.13.4 container, not just read from a compatibility table.
# Held at 8.19.3 deliberately rather than bumped to 9.x: nothing here
# uses a 9.x-only API, and per .github/dependabot.yml the pin moves only
# with a fresh recorded verification. See #62.
#
# #593: a Dependabot PR (build(deps): bump elasticsearch from 8.19.3 to
# 9.4.1) bumped this to 9.4.1 anyway, directly contradicting this comment.
# Reproduced live: es.ping() -> HEAD / returns HTTP 400 against the actual
# running 8.13.4 server, exactly the media_type_header_exception this
# comment already describes. Reverted to 8.19.3.
#
# #174: happened a second time -- PR #851 ("bump elasticsearch from 8.19.3
# to 9.4.1 in /ml-worker") re-bumped this right back to 9.4.1, same
# contradiction, same break. Found live while deploying an unrelated
# ml-worker fix: `docker compose up -d --build` failed outright (this
# time on the numpy/numba conflict below, which had *also* regressed --
# never got far enough to re-hit the ES 400 this pin causes, but reverting
# it here regardless since it's the exact same disproven change repeating).
# Reverted to 8.19.3 again. See .github/dependabot.yml's ignore rule for
# this directory -- added alongside this revert so Dependabot stops
# proposing it a third time instead of just fixing it again by hand later.
elasticsearch==8.19.3

# ML core
scikit-learn==1.9.0
joblib==1.5.3
# 2.4.6. This rationale was re-derived 2026-08-27 against the interpreter
# ml-worker/Dockerfile actually ships -- python:3.14-slim, digest-pinned.
# Treat the Dockerfile's FROM, not any interpreter number in a comment, as
# the thing these pins are verified against: the base image moved 3.12 ->
# 3.14 under a dependabot digest bump (#1070) without this comment being
# revisited, and the text below went on arguing about a Python this image
# no longer runs. The deployed container has been Python 3.14.7 running
# exactly these pins since that retag.
#
# What holds 2.4.6 today is verification discipline, not a solver wall:
# the numba/llvmlite/pyod pins below no longer force it. numba 0.67.0's own
# metadata allows numpy<2.6 and declares Python 3.14 support (checked on
# PyPI, 2026-08-27), and with numpy left unpinned pip resolves numpy 2.5.2
# on 3.14 and this whole file still installs -- so a numpy bump past 2.5 is
# NOT the ResolutionImpossible it was in the #593/#174 era below, when
# numba was still 0.66.0 with its numpy<2.5 cap. It stays 2.4.6 anyway,
# deliberately: this exact quartet (numpy 2.4.6 / numba 0.67.0 /
# llvmlite 0.49.0 / pyod 3.6.5) is the set verified to install together,
# re-run live on 2026-08-27 (clean `pip install` of this file on
# python:3.14.7, then import + numba njit smoke test), and numpy is in
# .github/dependabot.yml's ignore list for this directory precisely so it
# moves only with a fresh recorded joint verification, in lockstep with
# the numba/llvmlite pins below -- never numpy alone. See #61/#62 for the
# original verification; #593/#174 for what skipping that discipline costs.
#
# #593: a Dependabot PR (build(deps): bump numpy from 1.26.4 to 2.5.1)
# bumped this to 2.5.1 anyway, directly contradicting this comment, and
# broke the build exactly as predicted -- `numba 0.66.0 depends on
# numpy<2.5 and >=1.22`, ResolutionImpossible. Reverted back to 2.4.6.
# If numpy needs bumping again, the numba/llvmlite pins below need to move
# in lockstep (verify a clean joint install first), not numpy alone.
#
# #174: happened a second time -- PR #850 ("bump the python-compatible
# group across 1 directory with 2 updates") re-bumped this to 2.5.1 again,
# same contradiction, same break. Found live: `docker compose up -d
# --build` for an unrelated ml-worker fix failed outright with the exact
# ResolutionImpossible this comment already describes (numba 0.66.0 still
# pinned below, never independently bumped -- only numpy drifted).
# Reverted to 2.4.6 again. See .github/dependabot.yml's ignore rule for
# this directory -- added alongside this revert so Dependabot stops
# proposing it a third time instead of just fixing it again by hand later.
numpy==2.4.6
pandas==3.0.5

# Deep learning (CPU-only PyTorch)
torch==2.13.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Outlier detection (HBOS). Pulls in numba+llvmlite -- see the numpy pin
# above; this is the version set actually verified to install together.
pyod==3.6.5
numba==0.67.0
llvmlite==0.49.0

# Redis pub/sub
redis==8.1.0

# Utilities
python-dateutil==2.9.0.post0
requests==2.34.2
loguru==0.7.3
