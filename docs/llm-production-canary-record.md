# LLM production canary record (retired)

Removed from the tree by the archive sweep. This stub is kept so the pointers to
it elsewhere in the docs still resolve; the full text is recoverable from the
commit that removed it. See https://github.com/Xore/APIARY/issues/2458 for
context.
