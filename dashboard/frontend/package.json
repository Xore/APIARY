{
  "name": "honeypot-dashboard-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run build:api && npm run build:css",
    "build:api": "esbuild src/api.ts --bundle --format=iife --global-name=HoneypotAPI --outfile=../static/hp-api.js --minify",
    "build:css": "tailwindcss -i src/shell.css -o ../static/hp-tailwind.css --minify",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@tailwindcss/cli": "^4.3.3",
    "esbuild": "^0.28.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2"
  }
}
