{
  "name": "honeypot-dashboard-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run build:api",
    "build:api": "esbuild src/api.ts --bundle --format=iife --global-name=HoneypotAPI --outfile=../static/hp-api.js --minify",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2"
  }
}
