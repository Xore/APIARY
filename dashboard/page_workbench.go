package main

// pageWorkbench is a dedicated orchestration surface. Native Ghidra,
// sandbox, and deterministic result pages remain the canonical renderers.
var pageWorkbench = mustReadUI("payload_workbench.html")
