package main

// pageCape is /cape/{sha256}: a CAPE sandbox detonation result (#319),
// CAPE's own second, isolated detonation route alongside win11-sandbox
// (#322) -- purpose-built for debugger-class time evasion win11-sandbox's
// persona realism alone cannot defeat. Markup lives in ui/cape.html, not
// here -- see page_ghidra.go's own comment for why.
var pageCape = mustReadUI("cape.html")
