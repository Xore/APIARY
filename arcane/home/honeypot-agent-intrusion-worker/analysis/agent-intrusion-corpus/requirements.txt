# elasticsearch==9.5.0 -- tracks the deployed server's major: the ES fleet
# runs 9.5.1, digest-pinned in honeypot-init/compose.yml and
# honeypot-elk/compose.yml since #1408/#1410, and this exact client
# version re-verified live against it on 2026-08-26 (#2090): full
# info/ping/index/search/delete roundtrip PASS (recorded in
# docs/runtime-compatibility-record.md). A server accepts clients one
# major back but none forward -- so 9.x is right against today's server,
# while a future server DOWNGRADE to 8.x would break this pin the same
# way the old comments feared. This file is the cautionary tale behind
# that rule: dependabot bumped it to 9.5.0 while the server was still
# 8.13.4 (#872), a human reverted it hours before the server itself moved
# to 9.x (#1377 vs #1408/#1410), and dependabot re-bumped it the next day
# (#1516) -- correct only by luck of timing. The comment above each of
# those flips said something different; this one states the invariant:
# the pin may follow the server's major, never lead it, and moves only
# with a fresh recorded verification (.github/dependabot.yml ignores
# elasticsearch for this directory for exactly that reason).
elasticsearch==9.5.0
loguru==0.7.3
